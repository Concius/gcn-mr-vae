# Install PyTorch FIRST from the CUDA 12.8 index (RTX 5060 Ti is Blackwell / sm_120):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# then: pip install -r requirements.txt
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
tqdm
requests
hydra-core>=1.3
omegaconf>=2.3
pytest
# optional
# wandb
